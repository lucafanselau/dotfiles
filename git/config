[user]
	email = luca.fanselau@outlook.com
	name = Luca Fanselau

[pager]
	diff = delta
	show = delta
	log = delta
	reflog = delta

[interactive]
	diffFilter = delta --color-only

[pull]
	ff = only

# Machine-local secrets (tokens, etc.) go in ~/.config/git/config.local
[include]
	path = config.local
