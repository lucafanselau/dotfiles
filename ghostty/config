# Ghostty Configuration
# =====================

# One Dark theme (matches your Fish shell theme)
theme = "Atom One Dark"

# Fallback minimum contrast if theme still has issues
# Uncomment if you still see illegible text:
# minimum-contrast = 1.3

# Font (adjust to your preference)
font-size = 14
# font-family = "JetBrains Mono"

# Window
window-padding-x = 8
window-padding-y = 8

# Cursor
cursor-style = block
cursor-style-blink = false

# Mouse
mouse-hide-while-typing = true
minimum-contrast = 3

# Scrollback
scrollback-limit = 50000

# macOS specific
macos-option-as-alt = true
